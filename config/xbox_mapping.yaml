arm_teleop_node:
  ros__parameters:
    # Confirmed via ros2 topic echo /joy 
    axis_left_x: 0
    axis_left_y: 1
    axis_right_x: 3
    axis_right_y: 4
    axis_l2: 2
    axis_r2: 5

    # needs confirmation
    axis_dpad_x: 6
    axis_dpad_y: 7

    # Confirmed: A = button 0
    btn_enable: 0

    # needs confirmation
    btn_estop: 1
    btn_speed: 4